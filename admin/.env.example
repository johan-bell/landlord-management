# API base for fetch() (no trailing slash).
# - With Vite dev proxy (see vite.config): use /api so requests go to the same origin and proxy to the Nest server.
# - Or point directly at the API, e.g. http://localhost:3000
VITE_API_URL=/api

